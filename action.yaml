name: GitHub Action Updater
description: GitHub Action Updater updates GitHub action version and creates a pull request with the changes.
author: Maksudul Haque
branding:
  icon: 'upload-cloud'
  color: 'green'
inputs:
  committer_username:
    description: 'Username of that user who will commit'
    required: false
    default: 'github-actions[bot]'
  committer_email:
    description: 'Email Address of that user who will commit'
    required: false
    default: 'github-actions[bot]@users.noreply.github.com'
  ignore:
    description: 'JSON string with the list of actions that should be ignored'
    required: false
    default: '[]'
  token:
    description: 'Personal Access token with workflow permission'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
