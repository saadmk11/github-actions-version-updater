name: Comments
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3  # pin to v3 for stability
      - uses: actions/setup-node@v3 # node setup
      - uses: actions/cache@v3   # caching step