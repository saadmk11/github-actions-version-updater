name: Non-ASCII Comments
on: push
# ワークフロー定義
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # チェックアウト 🚀
      - uses: actions/setup-python@v5  # Configuración de Python – ñ é ü
