name: Comments
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # pin to v3 for stability
      - uses: actions/setup-node@v4 # node setup
      - uses: actions/cache@v4   # caching step
