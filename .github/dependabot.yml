version: 2
updates:
  # Action pins are updated by .github/workflows/update-actions.yaml.
  # Dependabot's github-actions ecosystem would open conflicting PRs.
  - package-ecosystem: "uv"
    directory: "/"
    groups:
       dependencies:
         patterns:
            - "*"
    schedule:
      interval: "monthly"
